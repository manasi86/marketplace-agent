{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"<code>marketplace-Agent</code> User Guide","text":"<p>Info</p> <p>This user guide is purely an illustrative example that shows off several features of Material for MkDocs and included Markdown extensions<sup>1</sup>.</p>"},{"location":"#installation","title":"Installation","text":"<p>First, install <code>uv</code>:</p> macOS and LinuxWindows <pre><code>curl -LsSf https://astral.sh/uv/install.sh | sh\n</code></pre> <pre><code>powershell -ExecutionPolicy ByPass -c \"irm https://astral.sh/uv/install.ps1 | iex\"\n</code></pre> <p>Then install the project and its dependencies:</p> <pre><code>uv sync\n</code></pre>"},{"location":"#quick-start","title":"Quick Start","text":"<p>To use <code>lib</code> as a library within your project, import it and execute the API like:</p> <pre><code>import lib\nimport api\n</code></pre> <p>Tip</p> <p>Within PyCharm, use Tab to auto-complete suggested imports while typing.</p> <ol> <li> <p>See <code>marketplace-Agent</code>'s <code>mkdocs.yml</code> for how to enable these features.\u00a0\u21a9</p> </li> </ol>"},{"location":"reference/lib/","title":"lib","text":""},{"location":"reference/lib/#lib","title":"<code>lib</code>","text":""},{"location":"reference/lib/sql_generator/","title":"sql_generator","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator","title":"<code>lib.sql_generator</code>","text":"<p>SQL generator agent built with LangGraph.</p>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.__all__","title":"<code>__all__ = ['AgentContext', 'AgentState', 'build_context', 'build_graph', 'initial_state', 'run_agent']</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext","title":"<code>AgentContext</code>  <code>dataclass</code>","text":"<p>Concrete dependencies bound at startup and used by all nodes.</p> Source code in <code>src/lib/sql_generator/context.py</code> <pre><code>@dataclass\nclass AgentContext:\n    \"\"\"Concrete dependencies bound at startup and used by all nodes.\"\"\"\n\n    settings: Settings\n    llm: LanguageModelLike\n    connection: OracleConnection\n    semantic: SemanticContext\n</code></pre>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext.connection","title":"<code>connection: OracleConnection</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext.llm","title":"<code>llm: LanguageModelLike</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext.semantic","title":"<code>semantic: SemanticContext</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext.settings","title":"<code>settings: Settings</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentContext.__init__","title":"<code>__init__(settings: Settings, llm: LanguageModelLike, connection: OracleConnection, semantic: SemanticContext) -&gt; None</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState","title":"<code>AgentState</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Mutable state threaded through every node in the LangGraph pipeline.</p> Source code in <code>src/lib/sql_generator/state.py</code> <pre><code>class AgentState(TypedDict):\n    \"\"\"Mutable state threaded through every node in the LangGraph pipeline.\"\"\"\n\n    user_query: str\n    intent: str\n    entities: dict[str, Any]\n    schema_hint: str | None\n    date_start: date | None\n    date_end: date | None\n    db_connected: bool\n    semantic_context: str\n    semantic_metadata: dict[str, Any]\n    sql_query: str\n    validation_error: str | None\n    attempt_count: int\n    max_attempts: int\n    query_columns: list[str] | None\n    query_rows: list[list[Any]] | None\n    execution_time_ms: float | None\n    logs: Annotated[list[tuple[str, str]], add]\n    error: str | None\n    done: bool\n</code></pre>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.attempt_count","title":"<code>attempt_count: int</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.date_end","title":"<code>date_end: date | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.date_start","title":"<code>date_start: date | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.db_connected","title":"<code>db_connected: bool</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.done","title":"<code>done: bool</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.entities","title":"<code>entities: dict[str, Any]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.error","title":"<code>error: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.execution_time_ms","title":"<code>execution_time_ms: float | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.intent","title":"<code>intent: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.logs","title":"<code>logs: Annotated[list[tuple[str, str]], add]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.max_attempts","title":"<code>max_attempts: int</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.query_columns","title":"<code>query_columns: list[str] | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.query_rows","title":"<code>query_rows: list[list[Any]] | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.schema_hint","title":"<code>schema_hint: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.semantic_context","title":"<code>semantic_context: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.semantic_metadata","title":"<code>semantic_metadata: dict[str, Any]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.sql_query","title":"<code>sql_query: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.user_query","title":"<code>user_query: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.AgentState.validation_error","title":"<code>validation_error: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/#lib.sql_generator.build_context","title":"<code>build_context(settings: Settings | None = None) -&gt; AgentContext</code>","text":"<p>Construct an AgentContext, resolving default settings if not supplied.</p> Source code in <code>src/lib/sql_generator/context.py</code> <pre><code>def build_context(settings: Settings | None = None) -&gt; AgentContext:\n    \"\"\"Construct an AgentContext, resolving default settings if not supplied.\"\"\"\n    resolved = settings or get_settings()\n    return AgentContext(\n        settings=resolved,\n        llm=get_llm(resolved),\n        connection=OracleConnection(resolved),\n        semantic=SemanticContext(),\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.build_graph","title":"<code>build_graph(context: AgentContext) -&gt; Any</code>","text":"<p>Compile the agent's StateGraph with traced node functions.</p> Source code in <code>src/lib/sql_generator/graph.py</code> <pre><code>def build_graph(context: AgentContext) -&gt; Any:\n    \"\"\"Compile the agent's StateGraph with traced node functions.\"\"\"\n    nodes = build_nodes(context)\n    graph = StateGraph(AgentState)\n    for name in NODE_NAMES:\n        graph.add_node(  # type: ignore[call-overload]\n            name, observe_step(name)(_log_step(name)(nodes[name]))\n        )\n\n    graph.set_entry_point(\"understand_intent\")\n    graph.add_edge(\"understand_intent\", \"check_db_connection\")\n    graph.add_conditional_edges(\n        \"check_db_connection\",\n        _route_after_connection,\n        {\"discover_semantic_layer\": \"discover_semantic_layer\", END: END},\n    )\n    graph.add_edge(\"discover_semantic_layer\", \"generate_sql\")\n    graph.add_edge(\"generate_sql\", \"validate_sql\")\n    graph.add_conditional_edges(\n        \"validate_sql\",\n        _route_after_validation,\n        {\n            \"generate_sql\": \"generate_sql\",\n            \"execute_and_display\": \"execute_and_display\",\n            END: END,\n        },\n    )\n    graph.add_edge(\"execute_and_display\", END)\n    return graph.compile()\n</code></pre>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.initial_state","title":"<code>initial_state(user_query: str, max_attempts: int) -&gt; AgentState</code>","text":"<p>Build a fresh AgentState with sensible defaults for a new run.</p> Source code in <code>src/lib/sql_generator/state.py</code> <pre><code>def initial_state(user_query: str, max_attempts: int) -&gt; AgentState:\n    \"\"\"Build a fresh AgentState with sensible defaults for a new run.\"\"\"\n    return AgentState(\n        user_query=user_query,\n        intent=\"\",\n        entities={},\n        schema_hint=None,\n        date_start=None,\n        date_end=None,\n        db_connected=False,\n        semantic_context=\"\",\n        semantic_metadata={},\n        sql_query=\"\",\n        validation_error=None,\n        attempt_count=0,\n        max_attempts=max_attempts,\n        query_columns=None,\n        query_rows=None,\n        execution_time_ms=None,\n        logs=[],\n        error=None,\n        done=False,\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/#lib.sql_generator.run_agent","title":"<code>run_agent(user_query: str, context: AgentContext) -&gt; AgentState</code>","text":"<p>Execute the full agent pipeline for a natural-language question.</p> Source code in <code>src/lib/sql_generator/graph.py</code> <pre><code>@observe_run(\"sql_generator_agent\")\ndef run_agent(user_query: str, context: AgentContext) -&gt; AgentState:\n    \"\"\"Execute the full agent pipeline for a natural-language question.\"\"\"\n    logger.info(\"Starting agent run for query: %s\", user_query)\n    graph = build_graph(context)\n    state = initial_state(user_query, context.settings.max_sql_attempts)\n    result = cast(AgentState, graph.invoke(state))\n    if result.get(\"error\") is not None:\n        logger.error(\"Agent run finished with an error: %s\", result[\"error\"])\n    else:\n        logger.info(\"Agent run finished successfully.\")\n    return result\n</code></pre>"},{"location":"reference/lib/sql_generator/cli/","title":"cli","text":""},{"location":"reference/lib/sql_generator/cli/#lib.sql_generator.cli","title":"<code>lib.sql_generator.cli</code>","text":"<p>Standalone CLI entry point for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/cli/#lib.sql_generator.cli.logger","title":"<code>logger = logging.getLogger(__name__)</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/cli/#lib.sql_generator.cli.main","title":"<code>main(argv: list[str] | None = None) -&gt; int</code>","text":"<p>Run the agent and render results; return a process exit code.</p> Source code in <code>src/lib/sql_generator/cli.py</code> <pre><code>def main(argv: list[str] | None = None) -&gt; int:\n    \"\"\"Run the agent and render results; return a process exit code.\"\"\"\n    load_dotenv()\n    args = parse_args(argv)\n    configure_logging(verbose=args.verbose)\n    settings = get_settings()\n    logger.debug(\n        \"Agent settings loaded: model=%s max_sql_attempts=%d\",\n        settings.sql_gen_model,\n        settings.max_sql_attempts,\n    )\n\n    if not settings.has_llm_credentials:\n        logger.error(\n            \"SQL_GEN_API_KEY is not set. Copy .env.example to .env and fill in your values.\"\n        )\n        return 2\n    if not settings.has_oracle_credentials:\n        logger.error(\"ORACLE_DSN, ORACLE_USER or ORACLE_PASSWORD missing. Update your .env file.\")\n        return 2\n\n    try:\n        context = build_context(settings)\n    except LLMError as exc:\n        logger.error(\"%s\", exc)\n        return 2\n\n    logger.info(\"Query: %s\", args.query)\n    state = run_agent(args.query, context)\n    print_agent_output(state)\n    if state.get(\"error\") is not None:\n        return 1\n    return 0\n</code></pre>"},{"location":"reference/lib/sql_generator/cli/#lib.sql_generator.cli.parse_args","title":"<code>parse_args(argv: list[str] | None = None) -&gt; argparse.Namespace</code>","text":"<p>Parse command-line arguments.</p> Source code in <code>src/lib/sql_generator/cli.py</code> <pre><code>def parse_args(argv: list[str] | None = None) -&gt; argparse.Namespace:\n    \"\"\"Parse command-line arguments.\"\"\"\n    parser = argparse.ArgumentParser(\n        prog=\"sql-generator\",\n        description=\"Generate and run Oracle SQL from a natural-language question.\",\n    )\n    parser.add_argument(\n        \"query\",\n        help=\"Natural-language question, e.g. 'Show total sales by region for Q1 2026'\",\n    )\n    parser.add_argument(\n        \"-v\",\n        \"--verbose\",\n        action=\"store_true\",\n        help=\"Show DEBUG-level logs (prompts, SQL and model responses) on stderr.\",\n    )\n    return parser.parse_args(argv)\n</code></pre>"},{"location":"reference/lib/sql_generator/config/","title":"config","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config","title":"<code>lib.sql_generator.config</code>","text":"<p>Environment-based configuration for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.MODULE_DIR","title":"<code>MODULE_DIR = Path(__file__).resolve().parent</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings","title":"<code>Settings</code>","text":"<p>               Bases: <code>BaseSettings</code></p> <p>Application settings loaded from environment variables and .env files.</p> Source code in <code>src/lib/sql_generator/config.py</code> <pre><code>class Settings(BaseSettings):\n    \"\"\"Application settings loaded from environment variables and .env files.\"\"\"\n\n    model_config = SettingsConfigDict(\n        env_file=(Path.cwd() / \".env\", MODULE_DIR / \".env\"),\n        env_file_encoding=\"utf-8\",\n        extra=\"ignore\",\n    )\n\n    # LLM.\n    sql_gen_base_url: str = \"https://llm.nalits.com/v1\"\n    sql_gen_model: str = \"gpt-4o\"\n    sql_gen_api_key: str = \"\"\n\n    # Oracle database.\n    oracle_dsn: str = \"\"\n    oracle_user: str = \"\"\n    oracle_password: str = \"\"\n\n    # Langfuse (self-hosted).\n    langfuse_host: str = \"\"\n    langfuse_public_key: str = \"\"\n    langfuse_secret_key: str = \"\"\n    langfuse_enabled: bool = True\n\n    # Agent behaviour.\n    max_sql_attempts: int = 3\n\n    @property\n    def has_oracle_credentials(self) -&gt; bool:\n        \"\"\"Return True when all Oracle connection settings are provided.\"\"\"\n        return bool(self.oracle_dsn and self.oracle_user and self.oracle_password)\n\n    @property\n    def has_llm_credentials(self) -&gt; bool:\n        \"\"\"Return True when an API key has been provided for the LLM.\"\"\"\n        return bool(self.sql_gen_api_key)\n\n    @property\n    def has_langfuse_credentials(self) -&gt; bool:\n        \"\"\"Return True when self-hosted Langfuse credentials are present.\"\"\"\n        return bool(self.langfuse_host and self.langfuse_public_key and self.langfuse_secret_key)\n</code></pre>"},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.has_langfuse_credentials","title":"<code>has_langfuse_credentials: bool</code>  <code>property</code>","text":"<p>Return True when self-hosted Langfuse credentials are present.</p>"},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.has_llm_credentials","title":"<code>has_llm_credentials: bool</code>  <code>property</code>","text":"<p>Return True when an API key has been provided for the LLM.</p>"},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.has_oracle_credentials","title":"<code>has_oracle_credentials: bool</code>  <code>property</code>","text":"<p>Return True when all Oracle connection settings are provided.</p>"},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.langfuse_enabled","title":"<code>langfuse_enabled: bool = True</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.langfuse_host","title":"<code>langfuse_host: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.langfuse_public_key","title":"<code>langfuse_public_key: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.langfuse_secret_key","title":"<code>langfuse_secret_key: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.max_sql_attempts","title":"<code>max_sql_attempts: int = 3</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.model_config","title":"<code>model_config = SettingsConfigDict(env_file=(Path.cwd() / '.env', MODULE_DIR / '.env'), env_file_encoding='utf-8', extra='ignore')</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.oracle_dsn","title":"<code>oracle_dsn: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.oracle_password","title":"<code>oracle_password: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.oracle_user","title":"<code>oracle_user: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.sql_gen_api_key","title":"<code>sql_gen_api_key: str = ''</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.sql_gen_base_url","title":"<code>sql_gen_base_url: str = 'https://llm.nalits.com/v1'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.Settings.sql_gen_model","title":"<code>sql_gen_model: str = 'gpt-4o'</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/config/#lib.sql_generator.config.get_settings","title":"<code>get_settings() -&gt; Settings</code>  <code>cached</code>","text":"<p>Return the cached application settings.</p> Source code in <code>src/lib/sql_generator/config.py</code> <pre><code>@lru_cache(maxsize=1)\ndef get_settings() -&gt; Settings:\n    \"\"\"Return the cached application settings.\"\"\"\n    return Settings()\n</code></pre>"},{"location":"reference/lib/sql_generator/context/","title":"context","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context","title":"<code>lib.sql_generator.context</code>","text":"<p>Runtime context shared by every graph node.</p>"},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext","title":"<code>AgentContext</code>  <code>dataclass</code>","text":"<p>Concrete dependencies bound at startup and used by all nodes.</p> Source code in <code>src/lib/sql_generator/context.py</code> <pre><code>@dataclass\nclass AgentContext:\n    \"\"\"Concrete dependencies bound at startup and used by all nodes.\"\"\"\n\n    settings: Settings\n    llm: LanguageModelLike\n    connection: OracleConnection\n    semantic: SemanticContext\n</code></pre>"},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext.connection","title":"<code>connection: OracleConnection</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext.llm","title":"<code>llm: LanguageModelLike</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext.semantic","title":"<code>semantic: SemanticContext</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext.settings","title":"<code>settings: Settings</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.AgentContext.__init__","title":"<code>__init__(settings: Settings, llm: LanguageModelLike, connection: OracleConnection, semantic: SemanticContext) -&gt; None</code>","text":""},{"location":"reference/lib/sql_generator/context/#lib.sql_generator.context.build_context","title":"<code>build_context(settings: Settings | None = None) -&gt; AgentContext</code>","text":"<p>Construct an AgentContext, resolving default settings if not supplied.</p> Source code in <code>src/lib/sql_generator/context.py</code> <pre><code>def build_context(settings: Settings | None = None) -&gt; AgentContext:\n    \"\"\"Construct an AgentContext, resolving default settings if not supplied.\"\"\"\n    resolved = settings or get_settings()\n    return AgentContext(\n        settings=resolved,\n        llm=get_llm(resolved),\n        connection=OracleConnection(resolved),\n        semantic=SemanticContext(),\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/dates/","title":"dates","text":""},{"location":"reference/lib/sql_generator/dates/#lib.sql_generator.dates","title":"<code>lib.sql_generator.dates</code>","text":"<p>Python date parsing and Oracle SQL literal formatting helpers.</p>"},{"location":"reference/lib/sql_generator/dates/#lib.sql_generator.dates.oracle_date_literal","title":"<code>oracle_date_literal(d: date) -&gt; str</code>","text":"<p>Format a Python date as an Oracle <code>TO_DATE</code> literal.</p> Source code in <code>src/lib/sql_generator/dates.py</code> <pre><code>def oracle_date_literal(d: date) -&gt; str:\n    \"\"\"Format a Python date as an Oracle ``TO_DATE`` literal.\"\"\"\n    return f\"TO_DATE('{d.isoformat()}', 'YYYY-MM-DD')\"\n</code></pre>"},{"location":"reference/lib/sql_generator/dates/#lib.sql_generator.dates.oracle_date_range","title":"<code>oracle_date_range(start: date | None, end: date | None) -&gt; str | None</code>","text":"<p>Build an Oracle date-range clause, or <code>None</code> if no date is available.</p> Source code in <code>src/lib/sql_generator/dates.py</code> <pre><code>def oracle_date_range(start: date | None, end: date | None) -&gt; str | None:\n    \"\"\"Build an Oracle date-range clause, or ``None`` if no date is available.\"\"\"\n    if start is not None and end is not None:\n        if start == end:\n            return oracle_date_literal(start)\n        return f\"{oracle_date_literal(start)} TO {oracle_date_literal(end)}\"\n    if start is not None:\n        return oracle_date_literal(start)\n    if end is not None:\n        return oracle_date_literal(end)\n    return None\n</code></pre>"},{"location":"reference/lib/sql_generator/dates/#lib.sql_generator.dates.parse_date_range","title":"<code>parse_date_range(entities: dict[str, Any]) -&gt; tuple[date | None, date | None]</code>","text":"<p>Parse the 'date_range' entity into (start, end) Python dates.</p> <p>Accepts single dates and ranges written as 'YYYY-MM-DD', with common separators like 'to', 'through', 'until' or '...'. Unparseable values yield <code>None</code> rather than raising.</p> Source code in <code>src/lib/sql_generator/dates.py</code> <pre><code>def parse_date_range(entities: dict[str, Any]) -&gt; tuple[date | None, date | None]:\n    \"\"\"Parse the 'date_range' entity into (start, end) Python dates.\n\n    Accepts single dates and ranges written as 'YYYY-MM-DD', with common\n    separators like 'to', 'through', 'until' or '...'. Unparseable values\n    yield ``None`` rather than raising.\n    \"\"\"\n    raw = entities.get(\"date_range\")\n    if not raw:\n        return (None, None)\n    parts = [part.strip() for part in _SEPARATOR.split(str(raw)) if part.strip()]\n    parsed = [d for part in parts if (d := _parse_iso(part)) is not None]\n    if not parsed:\n        return (None, None)\n    if len(parsed) == 1:\n        return (parsed[0], parsed[0])\n    return (parsed[0], parsed[-1])\n</code></pre>"},{"location":"reference/lib/sql_generator/db/","title":"db","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db","title":"<code>lib.sql_generator.db</code>","text":"<p>Oracle database access using oracledb in thin mode.</p>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.COLUMN_QUERY","title":"<code>COLUMN_QUERY = \"\\n    SELECT c.OWNER,\\n           c.TABLE_NAME,\\n           CASE WHEN v.VIEW_NAME IS NULL THEN 'TABLE' ELSE 'VIEW' END AS TABLE_TYPE,\\n           tc.COMMENTS AS TABLE_COMMENTS,\\n           c.COLUMN_NAME,\\n           c.DATA_TYPE,\\n           cc.COMMENTS AS COLUMN_COMMENTS\\n    FROM ALL_TAB_COLUMNS c\\n    LEFT JOIN ALL_VIEWS v\\n      ON c.OWNER = v.OWNER AND c.TABLE_NAME = v.VIEW_NAME\\n    LEFT JOIN ALL_TAB_COMMENTS tc\\n      ON c.OWNER = tc.OWNER AND c.TABLE_NAME = tc.TABLE_NAME\\n    LEFT JOIN ALL_COL_COMMENTS cc\\n      ON c.OWNER = cc.OWNER AND c.TABLE_NAME = cc.TABLE_NAME AND c.COLUMN_NAME = cc.COLUMN_NAME\\n    WHERE c.OWNER = :owner\\n    ORDER BY c.TABLE_NAME, c.COLUMN_ID\\n\"</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.SYSTEM_SCHEMAS","title":"<code>SYSTEM_SCHEMAS: tuple[str, ...] = ('SYS', 'SYSTEM', 'CTXSYS', 'MDSYS', 'XDB', 'LBACSYS', 'DVSYS', 'GSMADMIN_INTERNAL', 'OUTLN', 'DBSNMP', 'APPQOSSYS', 'WMSYS', 'EXFSYS', 'ORDSYS', 'OJVMTIM', 'ORACLE_OCM', 'AUDSYS', 'SYSBACKUP', 'SYSDG', 'SYSKM', 'SYSMAN', 'GGSYS', 'DIP', 'DVF', 'ODM', 'OWBSYS', 'SI_INFORMTN_SCHEMA', 'OLAPSYS', 'GSMROOT', 'SYS$UMF', 'REMOTE_SCHEDULER_AGENT', 'DBSFWUSER')</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.logger","title":"<code>logger = logging.getLogger(__name__)</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.DatabaseError","title":"<code>DatabaseError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when any database operation fails.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>class DatabaseError(Exception):\n    \"\"\"Raised when any database operation fails.\"\"\"\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection","title":"<code>OracleConnection</code>","text":"<p>Thin wrapper around a single oracledb connection.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>class OracleConnection:\n    \"\"\"Thin wrapper around a single oracledb connection.\"\"\"\n\n    def __init__(self, settings: Settings) -&gt; None:\n        \"\"\"Store settings; the connection is created lazily.\"\"\"\n        self._settings = settings\n        self._connection: oracledb.Connection | None = None\n\n    def connect(self) -&gt; None:\n        \"\"\"Open the connection if it is not already open.\"\"\"\n        if self._connection is not None:\n            return\n        if not self._settings.has_oracle_credentials:\n            raise DatabaseError(\n                \"Oracle credentials are missing. Set ORACLE_DSN, ORACLE_USER \"\n                \"and ORACLE_PASSWORD in the .env file.\"\n            )\n        try:\n            logger.info(\"Connecting to Oracle database...\")\n            self._connection = _connect_oracle(\n                user=self._settings.oracle_user,\n                password=self._settings.oracle_password,\n                dsn=self._settings.oracle_dsn,\n            )\n            logger.info(\"Connected to Oracle (dsn=%s)\", self._settings.oracle_dsn)\n        except oracledb.Error as exc:\n            logger.error(\"Failed to connect to Oracle: %s\", exc)\n            raise DatabaseError(f\"Failed to connect to Oracle: {exc}\") from exc\n\n    def check_connection(self) -&gt; bool:\n        \"\"\"Return True when the database responds to a trivial query.\"\"\"\n        try:\n            self.connect()\n        except DatabaseError:\n            return False\n        try:\n            self._execute(\"SELECT 1 FROM DUAL\")\n        except DatabaseError:\n            return False\n        return True\n\n    def execute_query(self, sql: str) -&gt; QueryResult:\n        \"\"\"Execute a SELECT statement and return its columns and rows.\"\"\"\n        self.connect()\n        logger.info(\"Executing query (%d chars)\", len(sql))\n        logger.debug(\"SQL:\\n%s\", sql)\n        try:\n            connection = self._connection\n            assert connection is not None\n            with connection.cursor() as cursor:\n                cursor.execute(sql)\n                if cursor.description is None:\n                    return QueryResult(columns=[], rows=[])\n                columns = [description[0] for description in cursor.description]\n                rows = [list(row) for row in cursor.fetchall()]\n                logger.info(\"Query complete: %d row(s)\", len(rows))\n                return QueryResult(columns=columns, rows=rows)\n        except oracledb.Error as exc:\n            logger.error(\"Query execution failed: %s\", exc)\n            raise DatabaseError(f\"Query execution failed: {exc}\") from exc\n\n    def explain_query(self, sql: str) -&gt; None:\n        \"\"\"Compile the query via EXPLAIN PLAN, raising on any error.\"\"\"\n        self.connect()\n        logger.debug(\"EXPLAIN PLAN FOR %s\", sql)\n        self._execute(f\"EXPLAIN PLAN FOR {sql}\")\n\n    def fetch_schema(self) -&gt; dict[str, Any]:\n        \"\"\"Discover business tables, views and columns from the data dictionary.\"\"\"\n        self.connect()\n        owners = self._fetch_owners()\n        logger.info(\"Fetching schema for %d owner(s): %s\", len(owners), \", \".join(owners))\n        schemas: dict[str, Any] = {}\n        for owner in owners:\n            schemas[owner] = {\"tables\": self._fetch_objects(owner)}\n        return schemas\n\n    def close(self) -&gt; None:\n        \"\"\"Close the underlying connection, if open.\"\"\"\n        if self._connection is not None:\n            self._connection.close()\n            self._connection = None\n\n    def _fetch_owners(self) -&gt; list[str]:\n        rows = self._query_dicts(\n            \"SELECT DISTINCT OWNER FROM ALL_TABLES WHERE OWNER NOT IN \" + self._placeholders(),\n            SYSTEM_SCHEMAS,\n        )\n        return sorted(str(row[\"OWNER\"]).upper() for row in rows if row.get(\"OWNER\"))\n\n    def _fetch_objects(self, owner: str) -&gt; dict[str, Any]:\n        rows = self._query_dicts(COLUMN_QUERY, {\"owner\": owner})\n        tables: dict[str, Any] = {}\n        for row in rows:\n            name = str(row[\"TABLE_NAME\"] or \"\").upper()\n            object_type = str(row[\"TABLE_TYPE\"] or \"TABLE\").upper()\n            table_comment = row[\"TABLE_COMMENTS\"]\n            column_name = str(row[\"COLUMN_NAME\"] or \"\").upper()\n            column_type = str(row[\"DATA_TYPE\"] or \"\").upper()\n            column_comment = row[\"COLUMN_COMMENTS\"]\n            tables.setdefault(\n                name,\n                {\"type\": object_type, \"description\": table_comment, \"columns\": {}},\n            )\n            tables[name][\"columns\"][column_name] = {\n                \"type\": column_type,\n                \"description\": column_comment,\n            }\n        return tables\n\n    def _query_dicts(\n        self,\n        sql: str,\n        parameters: Sequence[Any] | Mapping[str, Any] | None = None,\n    ) -&gt; list[dict[str, Any]]:\n        if self._connection is None:\n            raise DatabaseError(\"No active connection.\")\n        with self._connection.cursor() as cursor:\n            cursor.execute(sql, parameters) if parameters else cursor.execute(sql)\n            if cursor.description is None:\n                return []\n            columns = [description[0] for description in cursor.description]\n            return [dict(zip(columns, row, strict=True)) for row in cursor.fetchall()]\n\n    def _execute(self, sql: str) -&gt; None:\n        if self._connection is None:\n            raise DatabaseError(\"No active connection.\")\n        try:\n            with self._connection.cursor() as cursor:\n                cursor.execute(sql)\n        except oracledb.Error as exc:\n            raise DatabaseError(f\"SQL execution failed: {exc}\") from exc\n\n    def _placeholders(self) -&gt; str:\n        binds = \", \".join(f\":{index}\" for index in range(1, len(SYSTEM_SCHEMAS) + 1))\n        return f\"({binds})\"\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.__init__","title":"<code>__init__(settings: Settings) -&gt; None</code>","text":"<p>Store settings; the connection is created lazily.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def __init__(self, settings: Settings) -&gt; None:\n    \"\"\"Store settings; the connection is created lazily.\"\"\"\n    self._settings = settings\n    self._connection: oracledb.Connection | None = None\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.check_connection","title":"<code>check_connection() -&gt; bool</code>","text":"<p>Return True when the database responds to a trivial query.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def check_connection(self) -&gt; bool:\n    \"\"\"Return True when the database responds to a trivial query.\"\"\"\n    try:\n        self.connect()\n    except DatabaseError:\n        return False\n    try:\n        self._execute(\"SELECT 1 FROM DUAL\")\n    except DatabaseError:\n        return False\n    return True\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.close","title":"<code>close() -&gt; None</code>","text":"<p>Close the underlying connection, if open.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the underlying connection, if open.\"\"\"\n    if self._connection is not None:\n        self._connection.close()\n        self._connection = None\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.connect","title":"<code>connect() -&gt; None</code>","text":"<p>Open the connection if it is not already open.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def connect(self) -&gt; None:\n    \"\"\"Open the connection if it is not already open.\"\"\"\n    if self._connection is not None:\n        return\n    if not self._settings.has_oracle_credentials:\n        raise DatabaseError(\n            \"Oracle credentials are missing. Set ORACLE_DSN, ORACLE_USER \"\n            \"and ORACLE_PASSWORD in the .env file.\"\n        )\n    try:\n        logger.info(\"Connecting to Oracle database...\")\n        self._connection = _connect_oracle(\n            user=self._settings.oracle_user,\n            password=self._settings.oracle_password,\n            dsn=self._settings.oracle_dsn,\n        )\n        logger.info(\"Connected to Oracle (dsn=%s)\", self._settings.oracle_dsn)\n    except oracledb.Error as exc:\n        logger.error(\"Failed to connect to Oracle: %s\", exc)\n        raise DatabaseError(f\"Failed to connect to Oracle: {exc}\") from exc\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.execute_query","title":"<code>execute_query(sql: str) -&gt; QueryResult</code>","text":"<p>Execute a SELECT statement and return its columns and rows.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def execute_query(self, sql: str) -&gt; QueryResult:\n    \"\"\"Execute a SELECT statement and return its columns and rows.\"\"\"\n    self.connect()\n    logger.info(\"Executing query (%d chars)\", len(sql))\n    logger.debug(\"SQL:\\n%s\", sql)\n    try:\n        connection = self._connection\n        assert connection is not None\n        with connection.cursor() as cursor:\n            cursor.execute(sql)\n            if cursor.description is None:\n                return QueryResult(columns=[], rows=[])\n            columns = [description[0] for description in cursor.description]\n            rows = [list(row) for row in cursor.fetchall()]\n            logger.info(\"Query complete: %d row(s)\", len(rows))\n            return QueryResult(columns=columns, rows=rows)\n    except oracledb.Error as exc:\n        logger.error(\"Query execution failed: %s\", exc)\n        raise DatabaseError(f\"Query execution failed: {exc}\") from exc\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.explain_query","title":"<code>explain_query(sql: str) -&gt; None</code>","text":"<p>Compile the query via EXPLAIN PLAN, raising on any error.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def explain_query(self, sql: str) -&gt; None:\n    \"\"\"Compile the query via EXPLAIN PLAN, raising on any error.\"\"\"\n    self.connect()\n    logger.debug(\"EXPLAIN PLAN FOR %s\", sql)\n    self._execute(f\"EXPLAIN PLAN FOR {sql}\")\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.OracleConnection.fetch_schema","title":"<code>fetch_schema() -&gt; dict[str, Any]</code>","text":"<p>Discover business tables, views and columns from the data dictionary.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>def fetch_schema(self) -&gt; dict[str, Any]:\n    \"\"\"Discover business tables, views and columns from the data dictionary.\"\"\"\n    self.connect()\n    owners = self._fetch_owners()\n    logger.info(\"Fetching schema for %d owner(s): %s\", len(owners), \", \".join(owners))\n    schemas: dict[str, Any] = {}\n    for owner in owners:\n        schemas[owner] = {\"tables\": self._fetch_objects(owner)}\n    return schemas\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.QueryResult","title":"<code>QueryResult</code>  <code>dataclass</code>","text":"<p>Columns and rows returned by a successful SELECT statement.</p> Source code in <code>src/lib/sql_generator/db.py</code> <pre><code>@dataclass(frozen=True)\nclass QueryResult:\n    \"\"\"Columns and rows returned by a successful SELECT statement.\"\"\"\n\n    columns: list[str]\n    rows: list[list[Any]]\n</code></pre>"},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.QueryResult.columns","title":"<code>columns: list[str]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.QueryResult.rows","title":"<code>rows: list[list[Any]]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/db/#lib.sql_generator.db.QueryResult.__init__","title":"<code>__init__(columns: list[str], rows: list[list[Any]]) -&gt; None</code>","text":""},{"location":"reference/lib/sql_generator/display/","title":"display","text":""},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display","title":"<code>lib.sql_generator.display</code>","text":"<p>Pretty CLI rendering with Rich for agent logs and query results.</p>"},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display.NODE_TITLES","title":"<code>NODE_TITLES = {'understand_intent': '1. Understand Intent', 'check_db_connection': '2. Check DB Connection', 'discover_semantic_layer': '3. Discover Semantic Layer', 'generate_sql': '4. Generate SQL', 'validate_sql': '5. Validate SQL', 'execute_and_display': '6. Execute &amp; Display'}</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display.build_error_panel","title":"<code>build_error_panel(state: AgentState) -&gt; Panel</code>","text":"<p>Render the fatal error or final validation failure.</p> Source code in <code>src/lib/sql_generator/display.py</code> <pre><code>def build_error_panel(state: AgentState) -&gt; Panel:\n    \"\"\"Render the fatal error or final validation failure.\"\"\"\n    error = state.get(\"error\")\n    final_validation_error = state.get(\"validation_error\")\n    message = error or final_validation_error or \"Unknown error.\"\n    return Panel(Text(str(message), style=\"bold red\"), title=\"Failure\", border_style=\"red\")\n</code></pre>"},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display.build_log_panels","title":"<code>build_log_panels(state: AgentState) -&gt; list[Panel]</code>","text":"<p>Render each node's collected logs as its own panel.</p> Source code in <code>src/lib/sql_generator/display.py</code> <pre><code>def build_log_panels(state: AgentState) -&gt; list[Panel]:\n    \"\"\"Render each node's collected logs as its own panel.\"\"\"\n    grouped: OrderedDict[str, list[tuple[int, str]]] = OrderedDict()\n    for index, (node_name, message) in enumerate(state.get(\"logs\") or [], start=1):\n        grouped.setdefault(node_name, []).append((index, message))\n    panels: list[Panel] = []\n    for node_name, entries in grouped.items():\n        lines = [f\"[dim]{index:&gt;3}.[/dim] {message}\" for index, message in entries]\n        panels.append(\n            Panel(\n                \"\\n\".join(lines),\n                title=NODE_TITLES.get(node_name, node_name),\n                border_style=\"cyan\",\n            )\n        )\n    return panels\n</code></pre>"},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display.build_result_table","title":"<code>build_result_table(state: AgentState) -&gt; Table</code>","text":"<p>Build a Rich table for query results and metadata.</p> Source code in <code>src/lib/sql_generator/display.py</code> <pre><code>def build_result_table(state: AgentState) -&gt; Table:\n    \"\"\"Build a Rich table for query results and metadata.\"\"\"\n    columns = state.get(\"query_columns\") or []\n    rows = state.get(\"query_rows\") or []\n    table = Table(title=\"Query Results\", show_lines=False)\n    for column in columns:\n        table.add_column(str(column), overflow=\"fold\")\n    for row in rows:\n        table.add_row(*(str(cell) for cell in row))\n    table.caption = _metadata_caption(state)\n    return table\n</code></pre>"},{"location":"reference/lib/sql_generator/display/#lib.sql_generator.display.print_agent_output","title":"<code>print_agent_output(state: AgentState, console: Console | None = None) -&gt; None</code>","text":"<p>Print the full agent output: per-node logs, results (or error).</p> Source code in <code>src/lib/sql_generator/display.py</code> <pre><code>def print_agent_output(state: AgentState, console: Console | None = None) -&gt; None:\n    \"\"\"Print the full agent output: per-node logs, results (or error).\"\"\"\n    output_console = console or Console()\n    for panel in build_log_panels(state):\n        output_console.print(panel)\n    if state.get(\"query_rows\") is not None:\n        output_console.print(build_result_table(state))\n        return\n    if state.get(\"error\") is not None or state.get(\"validation_error\") is not None:\n        output_console.print(build_error_panel(state))\n        return\n    output_console.print(Text(\"Agent finished without producing a result.\", style=\"yellow\"))\n</code></pre>"},{"location":"reference/lib/sql_generator/graph/","title":"graph","text":""},{"location":"reference/lib/sql_generator/graph/#lib.sql_generator.graph","title":"<code>lib.sql_generator.graph</code>","text":"<p>LangGraph wiring for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/graph/#lib.sql_generator.graph.NODE_NAMES","title":"<code>NODE_NAMES = ('understand_intent', 'check_db_connection', 'discover_semantic_layer', 'generate_sql', 'validate_sql', 'execute_and_display')</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/graph/#lib.sql_generator.graph.logger","title":"<code>logger = logging.getLogger(__name__)</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/graph/#lib.sql_generator.graph.build_graph","title":"<code>build_graph(context: AgentContext) -&gt; Any</code>","text":"<p>Compile the agent's StateGraph with traced node functions.</p> Source code in <code>src/lib/sql_generator/graph.py</code> <pre><code>def build_graph(context: AgentContext) -&gt; Any:\n    \"\"\"Compile the agent's StateGraph with traced node functions.\"\"\"\n    nodes = build_nodes(context)\n    graph = StateGraph(AgentState)\n    for name in NODE_NAMES:\n        graph.add_node(  # type: ignore[call-overload]\n            name, observe_step(name)(_log_step(name)(nodes[name]))\n        )\n\n    graph.set_entry_point(\"understand_intent\")\n    graph.add_edge(\"understand_intent\", \"check_db_connection\")\n    graph.add_conditional_edges(\n        \"check_db_connection\",\n        _route_after_connection,\n        {\"discover_semantic_layer\": \"discover_semantic_layer\", END: END},\n    )\n    graph.add_edge(\"discover_semantic_layer\", \"generate_sql\")\n    graph.add_edge(\"generate_sql\", \"validate_sql\")\n    graph.add_conditional_edges(\n        \"validate_sql\",\n        _route_after_validation,\n        {\n            \"generate_sql\": \"generate_sql\",\n            \"execute_and_display\": \"execute_and_display\",\n            END: END,\n        },\n    )\n    graph.add_edge(\"execute_and_display\", END)\n    return graph.compile()\n</code></pre>"},{"location":"reference/lib/sql_generator/graph/#lib.sql_generator.graph.run_agent","title":"<code>run_agent(user_query: str, context: AgentContext) -&gt; AgentState</code>","text":"<p>Execute the full agent pipeline for a natural-language question.</p> Source code in <code>src/lib/sql_generator/graph.py</code> <pre><code>@observe_run(\"sql_generator_agent\")\ndef run_agent(user_query: str, context: AgentContext) -&gt; AgentState:\n    \"\"\"Execute the full agent pipeline for a natural-language question.\"\"\"\n    logger.info(\"Starting agent run for query: %s\", user_query)\n    graph = build_graph(context)\n    state = initial_state(user_query, context.settings.max_sql_attempts)\n    result = cast(AgentState, graph.invoke(state))\n    if result.get(\"error\") is not None:\n        logger.error(\"Agent run finished with an error: %s\", result[\"error\"])\n    else:\n        logger.info(\"Agent run finished successfully.\")\n    return result\n</code></pre>"},{"location":"reference/lib/sql_generator/llm/","title":"llm","text":""},{"location":"reference/lib/sql_generator/llm/#lib.sql_generator.llm","title":"<code>lib.sql_generator.llm</code>","text":"<p>OpenAI-compatible LLM client factory.</p>"},{"location":"reference/lib/sql_generator/llm/#lib.sql_generator.llm.logger","title":"<code>logger = logging.getLogger(__name__)</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/llm/#lib.sql_generator.llm.LLMError","title":"<code>LLMError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the LLM cannot be configured or invoked.</p> Source code in <code>src/lib/sql_generator/llm.py</code> <pre><code>class LLMError(Exception):\n    \"\"\"Raised when the LLM cannot be configured or invoked.\"\"\"\n</code></pre>"},{"location":"reference/lib/sql_generator/llm/#lib.sql_generator.llm.get_llm","title":"<code>get_llm(settings: Settings) -&gt; ChatOpenAI</code>","text":"<p>Return a ChatOpenAI client pointed at the configured endpoint.</p> Source code in <code>src/lib/sql_generator/llm.py</code> <pre><code>def get_llm(settings: Settings) -&gt; ChatOpenAI:\n    \"\"\"Return a ChatOpenAI client pointed at the configured endpoint.\"\"\"\n    if not settings.has_llm_credentials:\n        raise LLMError(\"Missing LLM API key. Set SQL_GEN_API_KEY in the .env file.\")\n    logger.info(\n        \"Initializing LLM client (model=%s, base_url=%s)\",\n        settings.sql_gen_model,\n        settings.sql_gen_base_url,\n    )\n    return ChatOpenAI(\n        model=settings.sql_gen_model,\n        api_key=SecretStr(settings.sql_gen_api_key),\n        base_url=settings.sql_gen_base_url,\n        temperature=0,\n        timeout=60,\n        max_retries=2,\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/llm/#lib.sql_generator.llm.invoke_llm","title":"<code>invoke_llm(llm: LanguageModelLike, prompt: str) -&gt; str</code>","text":"<p>Invoke the model with a prompt and return the trimmed text response.</p> Source code in <code>src/lib/sql_generator/llm.py</code> <pre><code>def invoke_llm(llm: LanguageModelLike, prompt: str) -&gt; str:\n    \"\"\"Invoke the model with a prompt and return the trimmed text response.\"\"\"\n    model_name = getattr(llm, \"model_name\", None) or llm.__class__.__name__\n    logger.info(\"LLM invoke (%s, %d chars in prompt)\", model_name, len(prompt))\n    logger.debug(\"Prompt:\\n%s\", prompt)\n    response = llm.invoke(prompt)\n    if not isinstance(response, str):\n        # LangChain message objects expose their content via .content.\n        content = getattr(response, \"content\", None)\n        if content is None:\n            raise LLMError(\"Model returned an empty response.\")\n        response = str(content).strip()\n    else:\n        response = response.strip()\n    logger.debug(\"Response:\\n%s\", response)\n    return response\n</code></pre>"},{"location":"reference/lib/sql_generator/logging_setup/","title":"logging_setup","text":""},{"location":"reference/lib/sql_generator/logging_setup/#lib.sql_generator.logging_setup","title":"<code>lib.sql_generator.logging_setup</code>","text":"<p>Console logging configuration for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/logging_setup/#lib.sql_generator.logging_setup.DATE_FORMAT","title":"<code>DATE_FORMAT = '%Y-%m-%d %H:%M:%S'</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/logging_setup/#lib.sql_generator.logging_setup.LOG_FORMAT","title":"<code>LOG_FORMAT = '%(asctime)s | %(levelname)-8s | %(name)s | %(message)s'</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/logging_setup/#lib.sql_generator.logging_setup.configure_logging","title":"<code>configure_logging(*, verbose: bool = False) -&gt; None</code>","text":"<p>Install a detailed console logger and set the root log level.</p> <p><code>verbose=False</code> selects INFO, <code>verbose=True</code> selects DEBUG (which also surfaces full prompts, SQL and model responses). Quiet third-party loggers are bumped to WARNING so the terminal stays readable.</p> Source code in <code>src/lib/sql_generator/logging_setup.py</code> <pre><code>def configure_logging(*, verbose: bool = False) -&gt; None:\n    \"\"\"Install a detailed console logger and set the root log level.\n\n    ``verbose=False`` selects INFO, ``verbose=True`` selects DEBUG (which also\n    surfaces full prompts, SQL and model responses). Quiet third-party loggers\n    are bumped to WARNING so the terminal stays readable.\n    \"\"\"\n    level = logging.DEBUG if verbose else logging.INFO\n    root = logging.getLogger()\n    root.setLevel(level)\n    if not any(isinstance(handler, _ConsoleHandler) for handler in root.handlers):\n        console = _ConsoleHandler()\n        console.setFormatter(logging.Formatter(LOG_FORMAT, datefmt=DATE_FORMAT))\n        root.addHandler(console)\n    for handler in root.handlers:\n        handler.setLevel(level)\n    for name in _NOISY_LOGGERS:\n        logging.getLogger(name).setLevel(logging.WARNING)\n</code></pre>"},{"location":"reference/lib/sql_generator/nodes/","title":"nodes","text":""},{"location":"reference/lib/sql_generator/nodes/#lib.sql_generator.nodes","title":"<code>lib.sql_generator.nodes</code>","text":"<p>Graph node implementations for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/nodes/#lib.sql_generator.nodes.NodeFn","title":"<code>NodeFn = Callable[[AgentState], dict[str, Any]]</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/nodes/#lib.sql_generator.nodes.logger","title":"<code>logger = logging.getLogger(__name__)</code>  <code>module-attribute</code>","text":""},{"location":"reference/lib/sql_generator/nodes/#lib.sql_generator.nodes.build_nodes","title":"<code>build_nodes(context: AgentContext) -&gt; dict[str, NodeFn]</code>","text":"<p>Create the six graph nodes closed over the shared agent context.</p> Source code in <code>src/lib/sql_generator/nodes.py</code> <pre><code>def build_nodes(context: AgentContext) -&gt; dict[str, NodeFn]:\n    \"\"\"Create the six graph nodes closed over the shared agent context.\"\"\"\n\n    def understand_intent(state: AgentState) -&gt; dict[str, Any]:\n        logger.info(\"Understanding intent for query: %s\", state[\"user_query\"])\n        try:\n            raw = invoke_llm(context.llm, intent_prompt(state[\"user_query\"]))\n            parsed = _parse_intent_json(raw)\n        except LLMError:\n            logger.warning(\n                \"LLM intent parsing failed; falling back to the raw query.\",\n                exc_info=True,\n            )\n            parsed = {\n                \"intent\": state[\"user_query\"],\n                \"entities\": {},\n                \"schema_hint\": None,\n            }\n        schema_hint = parsed.get(\"schema_hint\")\n        entities = parsed.get(\"entities\") or {}\n        date_start, date_end = parse_date_range(entities)\n        logger.info(\n            \"Intent parsed: %r (schema_hint=%r, entities=%r)\",\n            parsed.get(\"intent\"),\n            schema_hint,\n            entities,\n        )\n        return {\n            \"intent\": str(parsed.get(\"intent\") or state[\"user_query\"]),\n            \"entities\": entities,\n            \"schema_hint\": (str(schema_hint) if schema_hint else None),\n            \"date_start\": date_start,\n            \"date_end\": date_end,\n            \"logs\": [\n                (\n                    \"understand_intent\",\n                    f\"Understood intent: {parsed.get('intent') or state['user_query']}\",\n                ),\n            ],\n        }\n\n    def check_db_connection(state: AgentState) -&gt; dict[str, Any]:\n        logger.info(\"Checking database connection...\")\n        connected = context.connection.check_connection()\n        if not connected:\n            logger.error(\"Database connection check failed.\")\n            return {\n                \"db_connected\": False,\n                \"error\": \"Could not connect to the Oracle database.\",\n                \"done\": True,\n                \"logs\": [(\"check_db_connection\", \"Database connection check: FAILED\")],\n            }\n        logger.info(\"Database connection check: OK\")\n        return {\n            \"db_connected\": True,\n            \"logs\": [(\"check_db_connection\", \"Database connection check: OK\")],\n        }\n\n    def discover_semantic_layer(state: AgentState) -&gt; dict[str, Any]:\n        logger.info(\n            \"Discovering semantic layer%s...\",\n            f\" (schema hint: {state['schema_hint']})\" if state.get(\"schema_hint\") else \"\",\n        )\n        metadata = context.semantic.discover(context.connection)\n        formatted = context.semantic.format_for_prompt(state.get(\"schema_hint\"))\n        schemas = list(metadata)\n        covered = f\" (schema hint: {state['schema_hint']})\" if state.get(\"schema_hint\") else \"\"\n        logger.info(\n            \"Discovered %d schema(s): %s%s\",\n            len(schemas),\n            \", \".join(sorted(schemas)),\n            covered,\n        )\n        return {\n            \"semantic_metadata\": metadata,\n            \"semantic_context\": formatted,\n            \"logs\": [\n                (\n                    \"discover_semantic_layer\",\n                    f\"Discovered semantic layer: {len(schemas)} schema(s){covered}\",\n                ),\n            ],\n        }\n\n    def generate_sql(state: AgentState) -&gt; dict[str, Any]:\n        attempt_note = (\n            \" (fix attempt after validation error)\" if state.get(\"validation_error\") else \"\"\n        )\n        logger.info(\"Generating SQL%s...\", attempt_note)\n        prompt = sql_generation_prompt(\n            state[\"intent\"],\n            state[\"entities\"],\n            state[\"semantic_context\"],\n            state.get(\"validation_error\"),\n            date_start=state.get(\"date_start\"),\n            date_end=state.get(\"date_end\"),\n        )\n        logger.debug(\"SQL generation prompt:\\n%s\", prompt)\n        try:\n            raw = invoke_llm(context.llm, prompt)\n        except LLMError as exc:\n            logger.error(\"SQL generation failed: %s\", exc)\n            return {\n                \"error\": str(exc),\n                \"done\": True,\n                \"logs\": [(\"generate_sql\", f\"SQL generation failed: {exc}\")],\n            }\n        sql = sanitize_sql(raw)\n        logger.debug(\"Raw model response:\\n%s\", raw)\n        logger.info(\"Generated SQL%s: %s\", attempt_note, sql)\n        return {\"sql_query\": sql, \"logs\": [(\"generate_sql\", f\"Generated SQL{attempt_note}\")]}\n\n    def validate_sql_query(state: AgentState) -&gt; dict[str, Any]:\n        sql = state[\"sql_query\"]\n        logger.info(\"Validating SQL (SELECT-only + EXPLAIN)...\")\n        valid, error_message = validate_sql(sql, context.connection)\n        if valid:\n            logger.info(\"SQL validation: PASSED\")\n            return {\n                \"validation_error\": None,\n                \"logs\": [(\"validate_sql\", \"SQL validation: PASSED (SELECT-only + EXPLAIN OK)\")],\n            }\n        attempts = state[\"attempt_count\"] + 1\n        logger.warning(\n            \"SQL validation FAILED (attempt %d/%d): %s\",\n            attempts,\n            state[\"max_attempts\"],\n            error_message,\n        )\n        result: dict[str, Any] = {\n            \"attempt_count\": attempts,\n            \"validation_error\": error_message,\n            \"logs\": [\n                (\n                    \"validate_sql\",\n                    (\n                        f\"SQL validation FAILED \"\n                        f\"(attempt {attempts}/{state['max_attempts']}): {error_message}\"\n                    ),\n                )\n            ],\n        }\n        if attempts &gt;= state[\"max_attempts\"]:\n            result[\"error\"] = (\n                f\"SQL validation failed after {state['max_attempts']} attempts. \"\n                f\"Last error: {error_message}\"\n            )\n            result[\"done\"] = True\n        return result\n\n    def execute_and_display(state: AgentState) -&gt; dict[str, Any]:\n        logger.info(\"Executing SQL query...\")\n        start = perf_counter()\n        try:\n            query_result = context.connection.execute_query(state[\"sql_query\"])\n        except DatabaseError as exc:\n            logger.error(\"Query execution failed: %s\", exc)\n            return {\n                \"error\": str(exc),\n                \"done\": True,\n                \"logs\": [(\"execute_and_display\", f\"Query execution failed: {exc}\")],\n            }\n        elapsed_ms = (perf_counter() - start) * 1000.0\n        logger.info(\n            \"Query executed successfully: %d row(s) in %.1f ms\",\n            len(query_result.rows),\n            elapsed_ms,\n        )\n        return {\n            \"query_columns\": query_result.columns,\n            \"query_rows\": query_result.rows,\n            \"execution_time_ms\": elapsed_ms,\n            \"done\": True,\n            \"logs\": [\n                (\n                    \"execute_and_display\",\n                    (\n                        f\"Query executed successfully: {len(query_result.rows)} row(s) \"\n                        f\"in {elapsed_ms:.1f} ms\"\n                    ),\n                ),\n            ],\n        }\n\n    return {\n        \"understand_intent\": understand_intent,\n        \"check_db_connection\": check_db_connection,\n        \"discover_semantic_layer\": discover_semantic_layer,\n        \"generate_sql\": generate_sql,\n        \"validate_sql\": validate_sql_query,\n        \"execute_and_display\": execute_and_display,\n    }\n</code></pre>"},{"location":"reference/lib/sql_generator/observability/","title":"observability","text":""},{"location":"reference/lib/sql_generator/observability/#lib.sql_generator.observability","title":"<code>lib.sql_generator.observability</code>","text":"<p>Langfuse observability integration for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/observability/#lib.sql_generator.observability.observe_run","title":"<code>observe_run(name: str) -&gt; Callable[[Callable[_P, _R]], Callable[_P, _R]]</code>","text":"<p>Return a decorator that traces the whole agent run as a root call.</p> Source code in <code>src/lib/sql_generator/observability.py</code> <pre><code>def observe_run(name: str) -&gt; Callable[[Callable[_P, _R]], Callable[_P, _R]]:\n    \"\"\"Return a decorator that traces the whole agent run as a root call.\"\"\"\n    return _trace_wrapper(name, as_type=\"chain\")\n</code></pre>"},{"location":"reference/lib/sql_generator/observability/#lib.sql_generator.observability.observe_step","title":"<code>observe_step(name: str) -&gt; Callable[[Callable[_P, _R]], Callable[_P, _R]]</code>","text":"<p>Return a decorator that traces a graph step with Langfuse.</p> <p>Whether tracing is active is decided at call time, so decorating graph functions early has no side effects when observability is disabled.</p> Source code in <code>src/lib/sql_generator/observability.py</code> <pre><code>def observe_step(name: str) -&gt; Callable[[Callable[_P, _R]], Callable[_P, _R]]:\n    \"\"\"Return a decorator that traces a graph step with Langfuse.\n\n    Whether tracing is active is decided at call time, so decorating graph\n    functions early has no side effects when observability is disabled.\n    \"\"\"\n    return _trace_wrapper(name, as_type=\"span\")\n</code></pre>"},{"location":"reference/lib/sql_generator/observability/#lib.sql_generator.observability.tracing_configured","title":"<code>tracing_configured() -&gt; bool</code>","text":"<p>Return True when Langfuse tracing is enabled and configured.</p> Source code in <code>src/lib/sql_generator/observability.py</code> <pre><code>def tracing_configured() -&gt; bool:\n    \"\"\"Return True when Langfuse tracing is enabled and configured.\"\"\"\n    return _tracing_enabled(get_settings())\n</code></pre>"},{"location":"reference/lib/sql_generator/prompts/","title":"prompts","text":""},{"location":"reference/lib/sql_generator/prompts/#lib.sql_generator.prompts","title":"<code>lib.sql_generator.prompts</code>","text":"<p>Prompt templates for the intent-parsing and SQL-generation LLM calls.</p>"},{"location":"reference/lib/sql_generator/prompts/#lib.sql_generator.prompts.intent_prompt","title":"<code>intent_prompt(user_query: str) -&gt; str</code>","text":"<p>Build the prompt that parses a natural-language question into intent.</p> Source code in <code>src/lib/sql_generator/prompts.py</code> <pre><code>def intent_prompt(user_query: str) -&gt; str:\n    \"\"\"Build the prompt that parses a natural-language question into intent.\"\"\"\n    return f\"\"\"You are a data analyst. Parse the user's question about Oracle database data.\n\nUser question:\n{user_query}\n\nRespond with ONLY a JSON object in this exact shape:\n{{\n  \"intent\": \"brief description of what the user wants\",\n  \"entities\": {{\n    \"date_range\": \"e.g. '2025-01-01 to 2025-12-31' or null\",\n    \"filters\": {{\n      \"column\": \"value\"\n    }}\n  }},\n  \"schema_hint\": \"likely schema or domain name, or null\"\n}}\n\nRules:\n- intent must be a short actionable phrase, e.g. 'aggregate sales by region'\n- entities may be empty; never invent values the user did not mention\n- date_range must use ISO format 'YYYY-MM-DD'; use a single date for \"today\"-style\n  requests; represent a range as 'AAAA-MM-DD to BBBB-MM-DD'\n- schema_hint should name a schema/domain if the question implies one, else null\n\"\"\"\n</code></pre>"},{"location":"reference/lib/sql_generator/prompts/#lib.sql_generator.prompts.sql_generation_prompt","title":"<code>sql_generation_prompt(intent: str, entities: dict[str, Any], semantic_context: str, validation_error: str | None = None, date_start: date | None = None, date_end: date | None = None) -&gt; str</code>","text":"<p>Build the prompt that generates an Oracle SELECT statement.</p> Source code in <code>src/lib/sql_generator/prompts.py</code> <pre><code>def sql_generation_prompt(\n    intent: str,\n    entities: dict[str, Any],\n    semantic_context: str,\n    validation_error: str | None = None,\n    date_start: date | None = None,\n    date_end: date | None = None,\n) -&gt; str:\n    \"\"\"Build the prompt that generates an Oracle SELECT statement.\"\"\"\n    parts = [\n        \"You are an Oracle SQL expert. Generate a single SELECT statement.\",\n        \"\",\n        f\"Intent: {intent}\",\n        f\"Entities: {_render_entities(entities)}\",\n        \"\",\n        \"Available tables/views and their columns:\",\n        semantic_context,\n        \"\",\n        \"Constraints:\",\n        \"- Use ONLY tables/views and columns listed above.\",\n        \"- The query must be valid Oracle SQL (observe VARCHAR2, NUMBER, DATE types).\",\n        \"- Start with SELECT or WITH; do not use a trailing semicolon.\",\n        \"- Do not include any explanatory text; output ONLY the SQL.\",\n    ]\n    date_clause = oracle_date_range(date_start, date_end)\n    if date_clause is not None:\n        parts.append(\"\")\n        parts.append(f\"Date range: filter by the date using {date_clause}.\")\n    if validation_error:\n        parts.append(\"\")\n        parts.append(\"Your previous attempt FAILED validation with this error:\")\n        parts.append(validation_error)\n        parts.append(\"Fix the query. Pay close attention to the error; do not repeat the mistake.\")\n    return \"\\n\".join(parts)\n</code></pre>"},{"location":"reference/lib/sql_generator/prompts/#lib.sql_generator.prompts.sql_generation_system_message","title":"<code>sql_generation_system_message() -&gt; str</code>","text":"<p>Return a system-style instruction for SQL generation.</p> Source code in <code>src/lib/sql_generator/prompts.py</code> <pre><code>def sql_generation_system_message() -&gt; str:\n    \"\"\"Return a system-style instruction for SQL generation.\"\"\"\n    return \"Generate only valid Oracle SQL SELECT statements. No explanations, no markdown.\"\n</code></pre>"},{"location":"reference/lib/sql_generator/semantic/","title":"semantic","text":""},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic","title":"<code>lib.sql_generator.semantic</code>","text":"<p>Semantic layer: discovery results plus prompt-friendly formatting.</p>"},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic.SemanticContext","title":"<code>SemanticContext</code>","text":"<p>Cached, schema-aware view of the Oracle data dictionary.</p> Source code in <code>src/lib/sql_generator/semantic.py</code> <pre><code>class SemanticContext:\n    \"\"\"Cached, schema-aware view of the Oracle data dictionary.\"\"\"\n\n    def __init__(self) -&gt; None:\n        \"\"\"Initialise with no metadata loaded yet.\"\"\"\n        self._metadata: dict[str, Any] | None = None\n\n    def discover(self, connection: OracleConnection) -&gt; dict[str, Any]:\n        \"\"\"Load the semantic metadata, caching it across calls.\"\"\"\n        if self._metadata is None:\n            self._metadata = connection.fetch_schema()\n        return self._metadata\n\n    @property\n    def metadata(self) -&gt; dict[str, Any]:\n        \"\"\"Return cached metadata, raising if discovery has not run yet.\"\"\"\n        if self._metadata is None:\n            raise RuntimeError(\"Semantic discovery has not been run yet.\")\n        return self._metadata\n\n    def format_for_prompt(self, schema_hint: str | None = None) -&gt; str:\n        \"\"\"Render a compact textual summary of relevant tables and columns.\n\n        When a schema hint is supplied, only that schema is included; otherwise\n        every discovered schema is rendered.\n        \"\"\"\n        metadata = self.metadata\n        schemas = self._select_schemas(metadata, schema_hint)\n        if not schemas:\n            return \"No tables or views found in the database.\"\n        lines: list[str] = []\n        for schema_name in sorted(schemas):\n            tables = schemas[schema_name].get(\"tables\", {})\n            if not tables:\n                continue\n            lines.append(f\"Schema: {schema_name}\")\n            for table_name in sorted(tables):\n                table = tables[table_name]\n                description = table.get(\"description\")\n                lines.append(f\"  {table['type']} {table_name}: {description or 'no comment'}\")\n                for column_name in sorted(table.get(\"columns\", {})):\n                    column = table[\"columns\"][column_name]\n                    lines.append(\n                        f\"    - {column_name} ({column['type']}): \"\n                        f\"{column.get('description') or 'no comment'}\"\n                    )\n        rendered = \"\\n\".join(lines)\n        if len(rendered) &gt; _MAX_FORMATTED_CHARS:\n            rendered = rendered[:_MAX_FORMATTED_CHARS] + (\n                f\"\\n... (semantic layer truncated at {_MAX_FORMATTED_CHARS} chars; \"\n                \"provide a schema hint to narrow it)\"\n            )\n        return rendered\n\n    def _select_schemas(\n        self,\n        metadata: dict[str, Any],\n        schema_hint: str | None,\n    ) -&gt; dict[str, Any]:\n        if not schema_hint:\n            return metadata\n        hint = schema_hint.strip().upper()\n        matches = {name: data for name, data in metadata.items() if hint in name.upper()}\n        return matches or metadata\n</code></pre>"},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic.SemanticContext.metadata","title":"<code>metadata: dict[str, Any]</code>  <code>property</code>","text":"<p>Return cached metadata, raising if discovery has not run yet.</p>"},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic.SemanticContext.__init__","title":"<code>__init__() -&gt; None</code>","text":"<p>Initialise with no metadata loaded yet.</p> Source code in <code>src/lib/sql_generator/semantic.py</code> <pre><code>def __init__(self) -&gt; None:\n    \"\"\"Initialise with no metadata loaded yet.\"\"\"\n    self._metadata: dict[str, Any] | None = None\n</code></pre>"},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic.SemanticContext.discover","title":"<code>discover(connection: OracleConnection) -&gt; dict[str, Any]</code>","text":"<p>Load the semantic metadata, caching it across calls.</p> Source code in <code>src/lib/sql_generator/semantic.py</code> <pre><code>def discover(self, connection: OracleConnection) -&gt; dict[str, Any]:\n    \"\"\"Load the semantic metadata, caching it across calls.\"\"\"\n    if self._metadata is None:\n        self._metadata = connection.fetch_schema()\n    return self._metadata\n</code></pre>"},{"location":"reference/lib/sql_generator/semantic/#lib.sql_generator.semantic.SemanticContext.format_for_prompt","title":"<code>format_for_prompt(schema_hint: str | None = None) -&gt; str</code>","text":"<p>Render a compact textual summary of relevant tables and columns.</p> <p>When a schema hint is supplied, only that schema is included; otherwise every discovered schema is rendered.</p> Source code in <code>src/lib/sql_generator/semantic.py</code> <pre><code>def format_for_prompt(self, schema_hint: str | None = None) -&gt; str:\n    \"\"\"Render a compact textual summary of relevant tables and columns.\n\n    When a schema hint is supplied, only that schema is included; otherwise\n    every discovered schema is rendered.\n    \"\"\"\n    metadata = self.metadata\n    schemas = self._select_schemas(metadata, schema_hint)\n    if not schemas:\n        return \"No tables or views found in the database.\"\n    lines: list[str] = []\n    for schema_name in sorted(schemas):\n        tables = schemas[schema_name].get(\"tables\", {})\n        if not tables:\n            continue\n        lines.append(f\"Schema: {schema_name}\")\n        for table_name in sorted(tables):\n            table = tables[table_name]\n            description = table.get(\"description\")\n            lines.append(f\"  {table['type']} {table_name}: {description or 'no comment'}\")\n            for column_name in sorted(table.get(\"columns\", {})):\n                column = table[\"columns\"][column_name]\n                lines.append(\n                    f\"    - {column_name} ({column['type']}): \"\n                    f\"{column.get('description') or 'no comment'}\"\n                )\n    rendered = \"\\n\".join(lines)\n    if len(rendered) &gt; _MAX_FORMATTED_CHARS:\n        rendered = rendered[:_MAX_FORMATTED_CHARS] + (\n            f\"\\n... (semantic layer truncated at {_MAX_FORMATTED_CHARS} chars; \"\n            \"provide a schema hint to narrow it)\"\n        )\n    return rendered\n</code></pre>"},{"location":"reference/lib/sql_generator/state/","title":"state","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state","title":"<code>lib.sql_generator.state</code>","text":"<p>Shared graph state for the SQL generator agent.</p>"},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState","title":"<code>AgentState</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Mutable state threaded through every node in the LangGraph pipeline.</p> Source code in <code>src/lib/sql_generator/state.py</code> <pre><code>class AgentState(TypedDict):\n    \"\"\"Mutable state threaded through every node in the LangGraph pipeline.\"\"\"\n\n    user_query: str\n    intent: str\n    entities: dict[str, Any]\n    schema_hint: str | None\n    date_start: date | None\n    date_end: date | None\n    db_connected: bool\n    semantic_context: str\n    semantic_metadata: dict[str, Any]\n    sql_query: str\n    validation_error: str | None\n    attempt_count: int\n    max_attempts: int\n    query_columns: list[str] | None\n    query_rows: list[list[Any]] | None\n    execution_time_ms: float | None\n    logs: Annotated[list[tuple[str, str]], add]\n    error: str | None\n    done: bool\n</code></pre>"},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.attempt_count","title":"<code>attempt_count: int</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.date_end","title":"<code>date_end: date | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.date_start","title":"<code>date_start: date | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.db_connected","title":"<code>db_connected: bool</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.done","title":"<code>done: bool</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.entities","title":"<code>entities: dict[str, Any]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.error","title":"<code>error: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.execution_time_ms","title":"<code>execution_time_ms: float | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.intent","title":"<code>intent: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.logs","title":"<code>logs: Annotated[list[tuple[str, str]], add]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.max_attempts","title":"<code>max_attempts: int</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.query_columns","title":"<code>query_columns: list[str] | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.query_rows","title":"<code>query_rows: list[list[Any]] | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.schema_hint","title":"<code>schema_hint: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.semantic_context","title":"<code>semantic_context: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.semantic_metadata","title":"<code>semantic_metadata: dict[str, Any]</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.sql_query","title":"<code>sql_query: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.user_query","title":"<code>user_query: str</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.AgentState.validation_error","title":"<code>validation_error: str | None</code>  <code>instance-attribute</code>","text":""},{"location":"reference/lib/sql_generator/state/#lib.sql_generator.state.initial_state","title":"<code>initial_state(user_query: str, max_attempts: int) -&gt; AgentState</code>","text":"<p>Build a fresh AgentState with sensible defaults for a new run.</p> Source code in <code>src/lib/sql_generator/state.py</code> <pre><code>def initial_state(user_query: str, max_attempts: int) -&gt; AgentState:\n    \"\"\"Build a fresh AgentState with sensible defaults for a new run.\"\"\"\n    return AgentState(\n        user_query=user_query,\n        intent=\"\",\n        entities={},\n        schema_hint=None,\n        date_start=None,\n        date_end=None,\n        db_connected=False,\n        semantic_context=\"\",\n        semantic_metadata={},\n        sql_query=\"\",\n        validation_error=None,\n        attempt_count=0,\n        max_attempts=max_attempts,\n        query_columns=None,\n        query_rows=None,\n        execution_time_ms=None,\n        logs=[],\n        error=None,\n        done=False,\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/validator/","title":"validator","text":""},{"location":"reference/lib/sql_generator/validator/#lib.sql_generator.validator","title":"<code>lib.sql_generator.validator</code>","text":"<p>Validation of generated SQL: read-only guard plus compilation check.</p>"},{"location":"reference/lib/sql_generator/validator/#lib.sql_generator.validator.is_read_only","title":"<code>is_read_only(sql: str) -&gt; bool</code>","text":"<p>Return True when the statement is a read-only SELECT or WITH query.</p> Source code in <code>src/lib/sql_generator/validator.py</code> <pre><code>def is_read_only(sql: str) -&gt; bool:\n    \"\"\"Return True when the statement is a read-only SELECT or WITH query.\"\"\"\n    normalized = _strip_comments(sql).strip()\n    return bool(\n        normalized\n        and _READ_ONLY_START.match(normalized)\n        and not _FORBIDDEN_KEYWORDS.search(normalized)\n    )\n</code></pre>"},{"location":"reference/lib/sql_generator/validator/#lib.sql_generator.validator.sanitize_sql","title":"<code>sanitize_sql(raw: str) -&gt; str</code>","text":"<p>Normalise a model response into a single executable SQL statement.</p> Source code in <code>src/lib/sql_generator/validator.py</code> <pre><code>def sanitize_sql(raw: str) -&gt; str:\n    \"\"\"Normalise a model response into a single executable SQL statement.\"\"\"\n    cleaned = _strip_fences(_strip_comments(raw))\n    lines: list[str] = []\n    for line in cleaned.splitlines():\n        processed = line.rstrip(\";\").strip()\n        if processed:\n            lines.append(processed)\n    return \"\\n\".join(lines).strip()\n</code></pre>"},{"location":"reference/lib/sql_generator/validator/#lib.sql_generator.validator.validate_sql","title":"<code>validate_sql(sql: str, connection: OracleConnection) -&gt; tuple[bool, str | None]</code>","text":"<p>Validate generated SQL before execution.</p> <p>Performs two checks:</p> <ol> <li>The statement must be a read-only SELECT (or a WITH/CTE that resolves to    a SELECT) and must not contain any mutating or administrative keywords.</li> <li>The statement must compile, verified via <code>EXPLAIN PLAN</code> on the live    Oracle connection (catches unknown tables, bad columns and syntax    errors surfaced by the parser).</li> </ol> <p>Returns <code>(valid, error_message)</code> where <code>error_message</code> is <code>None</code> when the statement passes every check.</p> Source code in <code>src/lib/sql_generator/validator.py</code> <pre><code>def validate_sql(sql: str, connection: OracleConnection) -&gt; tuple[bool, str | None]:\n    \"\"\"Validate generated SQL before execution.\n\n    Performs two checks:\n\n    1. The statement must be a read-only SELECT (or a WITH/CTE that resolves to\n       a SELECT) and must not contain any mutating or administrative keywords.\n    2. The statement must compile, verified via ``EXPLAIN PLAN`` on the live\n       Oracle connection (catches unknown tables, bad columns and syntax\n       errors surfaced by the parser).\n\n    Returns ``(valid, error_message)`` where ``error_message`` is ``None`` when\n    the statement passes every check.\n    \"\"\"\n    clean = _strip_comments(sql)\n    normalized = clean.strip()\n    if not normalized:\n        return False, \"Generated SQL is empty.\"\n    if not _READ_ONLY_START.match(normalized):\n        return False, \"Only SELECT statements are allowed.\"\n    if _FORBIDDEN_KEYWORDS.search(normalized):\n        match = _FORBIDDEN_KEYWORDS.search(normalized)\n        assert match is not None\n        return False, f\"Forbidden keyword detected: {match.group(0).split()[0].upper()}.\"\n    try:\n        connection.explain_query(sql)\n    except DatabaseError as exc:\n        return False, str(exc)\n    return True, None\n</code></pre>"},{"location":"reference/api/","title":"api","text":""},{"location":"reference/api/#api","title":"<code>api</code>","text":""}]}